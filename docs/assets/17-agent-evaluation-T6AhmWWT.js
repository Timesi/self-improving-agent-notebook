const n=`{
 "cells": [
  {
   "cell_type": "markdown",
   "id": "ef9671e57a0f",
   "metadata": {},
   "source": [
    "# 评测 Agent：能力、时长与价值的度量\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "be60f95bf901",
   "metadata": {},
   "source": [
    "> 前面各讲把 LLM 一步步做成了会思考、会调用工具、会规划的 Agent。工具箱已经不少，但有一个基本问题还没有正面回答：怎样判断一个 Agent 做得好不好。\\n",
    ">\\n",
    "> 这一节建立评测体系。我们先看传统 benchmark 的局限，再用三篇论文依次补齐三个组件：METR 用人类完成时长当标尺，GDPval 用真实经济任务做盲评，DeepScholar-Bench 让自动评分可靠可用。最后把三者的组件拼成一个最小评测 harness。\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "736e0d64387a",
   "metadata": {},
   "source": [
    "评测一个 Agent，本质是回答三个子问题：任务从哪来、怎样算做对、谁来打分。它们对应评测系统的三个组件：任务环境、评分函数、聚合统计。单测是最清楚的例子——输入输出确定，评分函数是一组断言，聚合统计就是通过率。Agent 的任务开放得多，三个组件各有模糊之处，评测也因此分成多个层次：任务分布是否真实、成功判据是否唯一、自动评分器是否可靠、这些误差本身有没有被校准。\\n",
    "\\n",
    "本讲的三篇论文各自补一个层次：METR 处理\\"任务时长跨度大\\"，GDPval 处理\\"判据不唯一\\"，DeepScholar-Bench 处理\\"自动评分不可靠\\"。先从第一层说起——固定题库上的分数，为什么不足以评测 Agent。\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "eca13c782ba5",
   "metadata": {},
   "source": [
    "## 1. 评测 Agent 的难点\\n",
    "\\n",
    "固定题库上的选择题分数，对 Agent 有三层问题。第一是饱和：HellaSwag 这类题目是反向筛出来专难模型的，模型很快接近满分，分数失去区分度。第二是没有统一标尺：不同 benchmark 的分数不可比，一个模型在 MMLU 上接近满分、在 SWE-bench 上吃力，从分数推不出它到底能做什么。第三是任务不真实：选择题的任务分布固定、评分方式固定，离真实工作很远。\\n",
    "\\n",
    "三个问题指向同一个事实：选择题分数无法翻译成\\"能做完几小时的工作\\"。这一点在长任务上尤其明显——先用一个玩具例子看分数如何掩盖能力差异。\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "7fb7d30336c0",
   "metadata": {},
   "outputs": [],
   "source": [
    "# 固定题库得分相近，长任务成功率却差出十倍：分数掩盖了能力差异\\n",
    "import numpy as np\\n",
    "\\n",
    "np.random.seed(42)\\n",
    "# 模拟 100 道选择题：两个模型都接近满分\\n",
    "exam_a = np.random.binomial(n=1, p=0.97, size=100).mean()\\n",
    "exam_b = np.random.binomial(n=1, p=0.96, size=100).mean()\\n",
    "\\n",
    "# 模拟长任务：按人类时长分桶，模型 B 在长时间任务上更强\\n",
    "durations = np.array([0.5, 1.0, 2.0, 4.0, 8.0])      # 单位：小时\\n",
    "rate_a = np.array([0.90, 0.70, 0.40, 0.10, 0.02])\\n",
    "rate_b = np.array([0.85, 0.78, 0.65, 0.45, 0.30])\\n",
    "\\n",
    "print(\\"选择题得分：模型 A=%.3f  模型 B=%.3f\\" % (exam_a, exam_b))\\n",
    "print(\\"8 小时任务成功率：模型 A=%.2f  模型 B=%.2f\\" % (rate_a[-1], rate_b[-1]))\\n",
    "print(\\"关键观察：选择题分数看不出差距，换成长任务维度两个模型立刻分开。\\")\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "df0bec50f243",
   "metadata": {},
   "source": [
    "## 2. 长程任务评测：用人类时长标度能力\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "d38262628811",
   "metadata": {},
   "source": [
    "赛道有多难，可以用\\"人类跑完需要多久\\"来度量。METR 把同样的思路用在 Agent 上：构造一套软件与机器学习任务，难度从秒级到 8 小时，再让人类专家完成每个任务，取完成时长的几何平均作为任务的难度 t。任务来源有三块：HCAST 覆盖 1 分钟到 30 小时的常规软件任务，RE-Bench 是 8 小时的机器学习研究任务，SW AA 是 1 到 30 秒的单步小任务。\\n",
    "\\n",
    "每个 Agent 在每个任务上跑若干次，得到成功率。把任务难度 t 和成功率 p 一起，用一条 logistic 曲线拟合：\\n",
    "\\n",
    "$$p(\\\\text{task}) = \\\\sigma\\\\!\\\\big((\\\\log h - \\\\log t)\\\\cdot \\\\beta\\\\big)$$\\n",
    "\\n",
    "这里 h 是模型的 50% 时间视野——它一半概率成功的人类时长，beta 控制曲线的陡峭程度。当 h 等于任务时长 t 时，$\\\\log h - \\\\log t = 0$，$\\\\sigma(0) = 0.5$，成功率恰为一半。先手算一遍这条曲线。\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "75af1acb8e1f",
   "metadata": {},
   "outputs": [],
   "source": [
    "# 手算 logistic：h 与 t 的相对位置决定成功率\\n",
    "import numpy as np\\n",
    "\\n",
    "def sigma(z):\\n",
    "    \\"\\"\\"logistic sigmoid 函数。\\"\\"\\"\\n",
    "    return 1.0 / (1.0 + np.exp(-z))\\n",
    "\\n",
    "def success_prob(log_h, log_t, beta):\\n",
    "    \\"\\"\\"按 METR 公式计算成功率。\\"\\"\\"\\n",
    "    return sigma((log_h - log_t) * beta)\\n",
    "\\n",
    "beta = 2.0\\n",
    "log_h = np.log(2.0)      # 该模型 50% 时间视野 h = 2 小时\\n",
    "\\n",
    "print(\\"h=t=2h    ：p = %.3f  （应当接近 0.5）\\" % success_prob(log_h, np.log(2.0), beta))\\n",
    "print(\\"t=8h（难）：p = %.3f\\" % success_prob(log_h, np.log(8.0), beta))\\n",
    "print(\\"t=0.5h（易）：p = %.3f\\" % success_prob(log_h, np.log(0.5), beta))\\n",
    "print(\\"关键观察：h=t 时 p=0.5，任务越长成功率越低，beta 控制下降速度。\\")\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "045457ca6f70",
   "metadata": {},
   "outputs": [],
   "source": [
    "# 合成\\"任务时长-成功率\\"数据，用 numpy 拟合 50% 时间视野\\n",
    "import numpy as np\\n",
    "from scipy.optimize import minimize\\n",
    "\\n",
    "def neg_log_likelihood(params, log_t, y):\\n",
    "    \\"\\"\\"负对数似然：params=(log h, beta)，y 是 0/1 成败。\\"\\"\\"\\n",
    "    log_h, beta = params\\n",
    "    p = 1.0 / (1.0 + np.exp(-(log_h - log_t) * beta))\\n",
    "    eps = 1e-12\\n",
    "    return -np.sum(y * np.log(p + eps) + (1 - y) * np.log(1 - p + eps))\\n",
    "\\n",
    "np.random.seed(42)\\n",
    "# 60 个任务，人类时长取 log 均匀分布，覆盖 1 分钟到 8 小时\\n",
    "log_t = np.random.uniform(np.log(1.0 / 60.0), np.log(8.0), size=60)\\n",
    "true_log_h = np.log(2.0)            # 真实视野 2 小时\\n",
    "true_beta = 2.5\\n",
    "p_true = 1.0 / (1.0 + np.exp(-(true_log_h - log_t) * true_beta))\\n",
    "y = np.random.binomial(n=1, p=p_true, size=60)      # 每个任务一次成败采样\\n",
    "\\n",
    "# 拟合：优化负对数似然，log h 与 beta 各给一个合理范围\\n",
    "init = np.array([np.log(1.0), 1.0])\\n",
    "res = minimize(neg_log_likelihood, init, args=(log_t, y),\\n",
    "               method=\\"L-BFGS-B\\", bounds=[(-10.0, 10.0), (0.05, 10.0)])\\n",
    "log_h_est, beta_est = res.x\\n",
    "\\n",
    "print(\\"真实 50%% 视野 h=2.0 小时，拟合 h=%.2f 小时\\" % np.exp(log_h_est))\\n",
    "print(\\"拟合 beta=%.2f（真实 %.2f）\\" % (beta_est, true_beta))\\n",
    "print(\\"关键观察：只靠\\\\\\"任务时长 + 成败记录\\\\\\"就能还原模型的能力标尺。\\")\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "3f784cfdbd6e",
   "metadata": {},
   "outputs": [],
   "source": [
    "# 能力-时长曲线：横轴是任务的人类时长，纵轴是成功率\\n",
    "import numpy as np\\n",
    "import matplotlib.pyplot as plt\\n",
    "\\n",
    "np.random.seed(42)\\n",
    "grid_t = np.logspace(np.log10(1.0 / 60.0), np.log10(8.0), 200)\\n",
    "p_fit = 1.0 / (1.0 + np.exp(-(log_h_est - np.log(grid_t)) * beta_est))\\n",
    "\\n",
    "plt.figure(figsize=(6, 4))\\n",
    "plt.scatter(np.exp(log_t), y + 0.02 * np.random.uniform(-1, 1, len(y)),\\n",
    "            s=20, alpha=0.6, label=\\"observed tasks\\")\\n",
    "plt.plot(grid_t, p_fit, color=\\"tab:red\\", label=\\"fitted curve\\")\\n",
    "plt.axvline(np.exp(log_h_est), color=\\"gray\\", linestyle=\\"--\\")\\n",
    "plt.text(np.exp(log_h_est) * 1.6, 0.55, \\"h=%.1fh\\" % np.exp(log_h_est),\\n",
    "         color=\\"gray\\", fontsize=9)\\n",
    "plt.xscale(\\"log\\")\\n",
    "plt.xlabel(\\"human task duration (hours)\\")\\n",
    "plt.ylabel(\\"success rate\\")\\n",
    "plt.ylim(-0.05, 1.05)\\n",
    "plt.legend()\\n",
    "plt.tight_layout()\\n",
    "plt.show()\\n",
    "print(\\"关键观察：拟合曲线穿过 50% 的位置，对应的横轴坐标就是时间视野。\\")\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "c70ec82c45cd",
   "metadata": {},
   "source": [
    "单个时间视野是一张快照，更有信息量的是它随发布日期的变化。METR 对一批模型各自拟合出 50% 时间视野，再对 log h 与发布日期做线性回归，把斜率换算成\\"翻倍天数\\"。结论是前沿模型的视野约每 207 天翻一倍，即大约 7 个月：GPT-2 只有 2 秒，o3 约有 110 分钟。\\n",
    "\\n",
    "趋势线的斜率比任何单个模型的高度更可信。不同模型在同一批任务套件上的误差高度相关，单独比较两个模型的绝对视野并不可靠，回归斜率把相关的误差一起吸收掉。\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "e88436898554",
   "metadata": {},
   "outputs": [],
   "source": [
    "# 对 log h 与发布日期做线性回归，把斜率换算成翻倍天数\\n",
    "import numpy as np\\n",
    "import matplotlib.pyplot as plt\\n",
    "\\n",
    "# 合成 8 个模型的发布月份（2019 年 1 月起）与对应 50% 时间视野\\n",
    "months_since = np.array([0, 6, 12, 24, 36, 48, 60, 72])\\n",
    "horizons = np.array([0.0006, 0.0015, 0.004, 0.01, 0.05, 0.25, 1.0, 1.8])\\n",
    "\\n",
    "log_h = np.log(horizons)\\n",
    "A = np.column_stack([np.ones_like(months_since), months_since])\\n",
    "coef, *_ = np.linalg.lstsq(A, log_h, rcond=None)\\n",
    "intercept, slope = coef\\n",
    "\\n",
    "doubling_days = np.log(2.0) / slope * 30.0       # 每月按 30 天折算\\n",
    "\\n",
    "plt.figure(figsize=(6, 4))\\n",
    "plt.scatter(months_since, log_h, s=30, label=\\"models\\")\\n",
    "xs = np.array([0, 72])\\n",
    "plt.plot(xs, intercept + slope * xs, color=\\"tab:red\\", label=\\"OLS fit\\")\\n",
    "plt.xlabel(\\"months since 2019-01\\")\\n",
    "plt.ylabel(\\"log 50% horizon (hours)\\")\\n",
    "plt.legend()\\n",
    "plt.tight_layout()\\n",
    "plt.show()\\n",
    "\\n",
    "print(\\"翻倍天数 ≈ %.0f 天（METR 报告约 207 天）\\" % doubling_days)\\n",
    "print(\\"关键观察：log 尺度上的直线斜率，把能力进步压缩成一个数字。\\")\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "3494bde93573",
   "metadata": {},
   "source": [
    "## 3. 真实经济价值任务：盲评 win rate\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "2e7a8e4446c4",
   "metadata": {},
   "source": [
    "时间视野回答\\"能做多长的活\\"，没回答\\"做的活值多少钱、做得像不像样\\"。GDPval 换一个标尺：直接测真实经济任务。它从对 GDP 贡献最大的 9 大行业中，每个行业选出薪酬最高的 5 个数字化职业，共 44 个职业。职业专家把自己日常工作的真实产物改造成评测任务——每个任务是一份 request 加一份 deliverable，平均耗时约 9.5 小时，美元价值按\\"平均时长 × 职业时薪中位数\\"折算，平均约 398 美元。\\n",
    "\\n",
    "打分的办法是盲评：把模型的交付物和人类专家的交付物成对、匿名交给该职业的专家，判定谁更好。评分取三个值：模型赢记 1，平手记 0.5，模型输记 0。win rate 是这些得分的均值，也等于\\"赢的场次加半场平手\\"占总场次的比例。先手算一份成对比较表。\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "889d8206b91f",
   "metadata": {},
   "outputs": [],
   "source": [
    "# 手算盲评 win rate：三值评分 {赢 1, 平 0.5, 输 0}\\n",
    "import numpy as np\\n",
    "\\n",
    "# 10 个任务，每格是\\"模型 vs 人类专家\\"的比较结果\\n",
    "pairwise = np.array([1.0, 1.0, 0.5, 0.0, 1.0, 0.5, 1.0, 0.0, 0.5, 1.0])\\n",
    "wins = int(np.sum(pairwise == 1.0))\\n",
    "ties = int(np.sum(pairwise == 0.5))\\n",
    "losses = int(np.sum(pairwise == 0.0))\\n",
    "\\n",
    "win_rate = np.mean(pairwise)                  # 平手计 0.5 的期望得分\\n",
    "better_or_equal = (wins + ties) / len(pairwise)   # 达到或超过人类的占比\\n",
    "\\n",
    "print(\\"win=%d  tie=%d  loss=%d  （共 %d 个任务）\\" % (wins, ties, losses, len(pairwise)))\\n",
    "print(\\"win rate = %.2f\\" % win_rate)\\n",
    "print(\\"达到或超过人类 = %.2f\\" % better_or_equal)\\n",
    "print(\\"关键观察：平手计 0.5 计进 win rate，是\\\\\\"质量逼近人类\\\\\\"的保守口径。\\")\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "5edb7404431f",
   "metadata": {},
   "source": [
    "真专家盲评的成本很高，单次成对比较要 1 小时以上。GDPval 训练了一个自动评分器（LLM judge）来替代专家，但评分器靠不靠谱要先被度量。记 A 是自动评分器对某任务的 0/0.5/1 打分，H 是人类打分，一致率定义为\\n",
    "\\n",
    "$$A^{\\\\text{HA}} = E[\\\\,1 - |H - A|\\\\,]$$\\n",
    "\\n",
    "人类间一致率是同一任务的两份人类打分 H1、H2 之间的 $1 - |H_1 - H_2|$。这两条数要放在一起看：自动评分器和人类的一致率，不应该比人类彼此之间差太多。GDPval 里两者分别是 66% 与 71%。用一份小数据复现这个结构。\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "b2395fba5a5e",
   "metadata": {},
   "outputs": [],
   "source": [
    "# 一致率：自动评分器 vs 人类，人类 vs 人类\\n",
    "import numpy as np\\n",
    "\\n",
    "# 10 个任务：自动评分器打分 A，两位人类专家打分 H1、H2\\n",
    "A  = np.array([1.0, 1.0, 0.5, 0.0, 0.5, 1.0, 0.0, 0.0, 1.0, 0.5])\\n",
    "H1 = np.array([1.0, 0.5, 0.5, 0.0, 0.5, 1.0, 0.5, 0.0, 1.0, 1.0])\\n",
    "H2 = np.array([1.0, 0.5, 0.5, 0.0, 0.5, 1.0, 0.5, 0.0, 1.0, 0.5])\\n",
    "\\n",
    "def agreement(x, y):\\n",
    "    \\"\\"\\"两条评分的平均一致率 1 - |x - y|。\\"\\"\\"\\n",
    "    return np.mean(1.0 - np.abs(x - y))\\n",
    "\\n",
    "A_HA = agreement(A, (H1 + H2) / 2.0)      # 自动评分器 vs 人类平均打分\\n",
    "A_HH = agreement(H1, H2)                   # 人类彼此间\\n",
    "\\n",
    "print(\\"A_HA（自动评分器 vs 人类）= %.2f\\" % A_HA)\\n",
    "print(\\"A_HH（人类间）= %.2f\\" % A_HH)\\n",
    "print(\\"关键观察：自动评分器与人类的一致率略低于人类彼此间，这个差距要持续监控。\\")\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "b6cce16cf6ff",
   "metadata": {},
   "source": [
    "盲评回答\\"赢了多少场\\"，老板关心的是\\"交给模型做能省多少时间\\"。一个现实的工作流是\\"先用模型，不满意再自己修\\"。设模型在某个任务上的 win rate 为 w，模型完成耗时 M_T，专家审阅耗时 R_T，人类自己完成耗时 H_T。只试一次时，模型输出可接受就直接收（概率 w），不可接受就返工重做（概率 1-w），期望时间是\\n",
    "\\n",
    "$$E[T_{1,i}] = M_{T,i} + R_{T,i} + (1 - w_i)\\\\, H_{T,i}$$\\n",
    "\\n",
    "尝试 n 次时，几何级数封进公式：\\n",
    "\\n",
    "$$E[T_{n,i}] = (M_{T,i}+R_{T,i})\\\\,\\\\frac{1-(1-w_i)^n}{w_i} + (1-w_i)^n\\\\, H_{T,i}$$\\n",
    "\\n",
    "先手算 try-1 在几个 win rate 下的期望耗时，再看曲线。\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "341c9421927e",
   "metadata": {},
   "outputs": [],
   "source": [
    "# 手算 try-1 期望成本：模型协助 vs 纯人工\\n",
    "import numpy as np\\n",
    "\\n",
    "M_T = 0.5     # 模型完成：0.5 小时\\n",
    "R_T = 0.3     # 专家审阅：0.3 小时\\n",
    "H_T = 2.0     # 人类自己完成：2 小时\\n",
    "\\n",
    "def expected_time_try1(w):\\n",
    "    \\"\\"\\"只试一次：模型 + 审阅，失败则人类返工。\\"\\"\\"\\n",
    "    return M_T + R_T + (1 - w) * H_T\\n",
    "\\n",
    "for w in [0.2, 0.5, 0.8, 1.0]:\\n",
    "    print(\\"w=%.1f：期望 %.2f 小时（纯人工 %.1f 小时）\\" % (w, expected_time_try1(w), H_T))\\n",
    "\\n",
    "print(\\"关键观察：w=0.2 时模型协助反而更慢，收益全靠足够高的 win rate。\\")\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "e2688f3bc66a",
   "metadata": {},
   "outputs": [],
   "source": [
    "# try-n 成本曲线：扫描 win rate 与尝试次数，找交叉点\\n",
    "import numpy as np\\n",
    "import matplotlib.pyplot as plt\\n",
    "\\n",
    "M_T, R_T, H_T = 0.5, 0.3, 2.0\\n",
    "\\n",
    "def expected_time_tryn(w, n):\\n",
    "    \\"\\"\\"尝试 n 次的期望耗时；w 为 0 时退化为纯人工。\\"\\"\\"\\n",
    "    q = 1.0 - w\\n",
    "    if w <= 0:\\n",
    "        return H_T\\n",
    "    return (M_T + R_T) * (1 - q**n) / w + q**n * H_T\\n",
    "\\n",
    "ws = np.linspace(0.0, 1.0, 100)\\n",
    "plt.figure(figsize=(6, 4))\\n",
    "for n in [1, 3, 10]:\\n",
    "    plt.plot(ws, [expected_time_tryn(w, n) for w in ws], label=\\"try-%d\\" % n)\\n",
    "plt.axhline(H_T, color=\\"gray\\", linestyle=\\"--\\", label=\\"human only\\")\\n",
    "plt.axvline((M_T + R_T) / H_T, color=\\"gray\\", linestyle=\\":\\", label=\\"crossover\\")\\n",
    "plt.xlabel(\\"model win rate w\\")\\n",
    "plt.ylabel(\\"expected time (hours)\\")\\n",
    "plt.ylim(0, 2.5)\\n",
    "plt.legend()\\n",
    "plt.tight_layout()\\n",
    "plt.show()\\n",
    "print(\\"关键观察：win rate 超过 (M_T+R_T)/H_T=%.2f 后，模型协助才开始划算。\\"\\n",
    "      % ((M_T + R_T) / H_T))\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "02c57b028c0c",
   "metadata": {},
   "source": [
    "## 4. 深度研究评测：自动评分与它的漏洞\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "2ca51d223dbb",
   "metadata": {},
   "source": [
    "GDPval 已经用自动评分器替代专家，但评分器自身的可靠性要持续校准。DeepScholar-Bench 把自动评测推到极限：它要评测\\"深度研究\\"这类开放式长程任务，正确性没有唯一标准，于是全部指标交给 LLM judge。为了避免 benchmark 饱和与数据泄漏，它用持续更新的管道从近期接收的 arXiv 论文自动生成任务——给定论文标题和摘要，生成 related work 一节，人类作者写的原文就是参考答案。\\n",
    "\\n",
    "评测分三个维度：知识综合（organization、nugget coverage）、检索质量（relevance rate、reference coverage、document importance）、可验证性（citation precision、claim coverage）。每个指标都要和人类标注校准：用 11 位博士的 300 多条标注，LLM judge 与人类的组织一致性约 71%，nugget 标注约 83%。\\n",
    "\\n",
    "自动评分最典型的问题之一是位置偏差，先演示一遍。\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "b40c2155699d",
   "metadata": {},
   "outputs": [],
   "source": [
    "# LLM judge 的位置偏差：同一对答案，调换顺序后结论可能相反\\n",
    "import numpy as np\\n",
    "\\n",
    "def judge_first(text):\\n",
    "    \\"\\"\\"规则模拟 judge：无条件选择排在前面的答案。\\"\\"\\"\\n",
    "    pos_a = text.find(\\"ANSWER_A\\")\\n",
    "    pos_b = text.find(\\"ANSWER_B\\")\\n",
    "    return \\"A\\" if pos_a < pos_b else \\"B\\"\\n",
    "\\n",
    "def judge_pair(a, b):\\n",
    "    \\"\\"\\"同一对答案按两种顺序各评一次，返回两个胜者。\\"\\"\\"\\n",
    "    order1 = judge_first(\\"ANSWER_A=\\" + a + \\" ANSWER_B=\\" + b)\\n",
    "    order2 = judge_first(\\"ANSWER_B=\\" + a + \\" ANSWER_A=\\" + b)\\n",
    "    return order1, order2\\n",
    "\\n",
    "good = \\"结构清晰，覆盖要点。\\"\\n",
    "bad = \\"内容零散，遗漏要点。\\"\\n",
    "r1, r2 = judge_pair(good, bad)\\n",
    "\\n",
    "print(\\"顺序（好在前）：judge 判 %s 胜\\" % r1)\\n",
    "print(\\"顺序（好在后）：judge 判 %s 胜\\" % r2)\\n",
    "print(\\"关键观察：同一个 judge 因位置不同给出相反结论，单次打分不可信。\\")\\n",
    "\\n",
    "# swap 平均：两序打分不一致时按平手处理，摊平位置偏差\\n",
    "scores = [1.0, 0.0]        # 好答案两次比较的得分：一次赢、一次输\\n",
    "win_rate_swap = np.mean(scores)\\n",
    "print(\\"好答案两次得分：%s，swap 平均 win rate = %.2f\\" % (scores, win_rate_swap))\\n",
    "print(\\"关键观察：两序平均把方向性偏差摊成平手，抵消位置偏差。\\")\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "1f3a4ba759dc",
   "metadata": {},
   "source": [
    "研究综述的输出是一段文字加一串引用，评测看三个指标。相关性（relevance rate）：每个被引来源由 judge 打 0/1/2 分，RR 是平均分除以 2。引用覆盖（reference coverage）：人类 exemplar 里标出的重要引文，报告引到了多少，是重要引文的召回率。可验证性：句子里的主张有多少被该句的引用撑住（claim coverage）。三个指标分别回答\\"引得对不对、全不全、撑不撑得住\\"。\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "f9574a28ad2e",
   "metadata": {},
   "outputs": [],
   "source": [
    "# 检索质量 toy 计算：相关性 RR、引用覆盖 RC、可验证性 claim coverage\\n",
    "import numpy as np\\n",
    "\\n",
    "# 相关性：judge 给每个被引来源打 0/1/2 分\\n",
    "rels = np.array([2, 1, 2, 0, 2])\\n",
    "RR = rels.mean() / 2.0\\n",
    "\\n",
    "# 引用覆盖：exemplar 有 6 条重要引文，报告引到了 4 条\\n",
    "RC = 4 / 6\\n",
    "\\n",
    "# 可验证性：报告 8 个句子中，7 句的主张被其引用撑住\\n",
    "claim_cov = 7 / 8\\n",
    "\\n",
    "print(\\"Relevance Rate RR = %.3f（满分为 1）\\" % RR)\\n",
    "print(\\"Reference Coverage RC = %.3f（重要引文的召回率）\\" % RC)\\n",
    "print(\\"Verifiability（claim coverage）= %.3f\\" % claim_cov)\\n",
    "print(\\"关键观察：三个指标分别回答\\\\\\"引得对不对、全不全、撑不撑得住\\\\\\"。\\")\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "13154a2a1ff2",
   "metadata": {},
   "outputs": [],
   "source": [
    "# oracle 消融与几何平均：检索不是瓶颈，综合才是\\n",
    "import numpy as np\\n",
    "\\n",
    "# DeepScholar 报告的数字：把\\"正确答案应引的文献\\"直接喂给系统\\n",
    "base_rc = 0.187          # 原始引用覆盖\\n",
    "oracle_rc = 1.0\\n",
    "base_nugget = 0.392      # 原始 nugget 覆盖\\n",
    "oracle_nugget = 0.49\\n",
    "\\n",
    "print(\\"引用覆盖：%.3f -> %.3f\\" % (base_rc, oracle_rc))\\n",
    "print(\\"nugget 覆盖：%.3f -> %.3f\\" % (base_nugget, oracle_nugget))\\n",
    "print(\\"关键观察：检索全对，nugget 覆盖只涨了 0.1，瓶颈在综合不在检索。\\")\\n",
    "\\n",
    "def geomean(x):\\n",
    "    \\"\\"\\"几何平均；任何一项为 0 时返回 0。\\"\\"\\"\\n",
    "    if np.any(x <= 0):\\n",
    "        return 0.0\\n",
    "    return np.exp(np.mean(np.log(x)))\\n",
    "\\n",
    "system_c = np.array([0.95, 0.0, 0.95, 0.95, 0.95])\\n",
    "print(\\"一项为 0 的系统：算术平均 %.3f，几何平均 %.3f\\"\\n",
    "      % (system_c.mean(), geomean(system_c)))\\n",
    "print(\\"关键观察：几何平均惩罚短板，单项为 0 会让总分归零。\\")\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "19bb9b35fbe5",
   "metadata": {},
   "source": [
    "## 5. 构建自己的评测 Harness\\n",
    "\\n",
    "前三节的工具各解决一个组件：时间视野处理\\"任务难度跨度大\\"，win rate 与一致率处理\\"判据不唯一\\"，LLM judge 处理\\"评分自动化\\"，同时要防它的偏差。把它们拼起来就是评测 harness——任务环境、评分函数、聚合统计三块各就各位。下面用一个 toy 任务套件走一遍完整流程。\\n",
    "\\n",
    "**运行说明**：下面的 agent 用 llm_client 驱动，mock 模式下由脚本化的确定性动作执行，真实模式下由模型输出动作。两种情况 harness 的判定逻辑相同。\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "d4bf4135d56e",
   "metadata": {},
   "outputs": [],
   "source": [
    "# 统一 LLM 客户端：有 API key 用真实模型，否则自动进入 mock 模式\\n",
    "import sys, os\\n",
    "_root = os.path.abspath(os.getcwd())\\n",
    "while not os.path.exists(os.path.join(_root, 'llm_client.py')):\\n",
    "    _root = os.path.dirname(_root)\\n",
    "    if _root == os.path.dirname(_root):\\n",
    "        break\\n",
    "if _root not in sys.path:\\n",
    "    sys.path.insert(0, _root)\\n",
    "from llm_client import get_llm\\n",
    "client = get_llm()\\n",
    "print(\\"LLM 模式：\\", \\"mock（确定性占位输出）\\" if client.is_mock else \\"real\\")\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "3d6482fd5e57",
   "metadata": {},
   "outputs": [],
   "source": [
    "# 评测 harness 第一块：任务环境（toy 任务套件，可程序判对错）\\n",
    "# 每个任务 = (名称, 参数, 期望结果)，评分就是\\"输出是否等于期望\\"\\n",
    "TASKS = [\\n",
    "    (\\"求和\\", (3, 5), 8),\\n",
    "    (\\"求和\\", (12, 7), 19),\\n",
    "    (\\"差\\", (10, 4), 6),\\n",
    "    (\\"差\\", (20, 9), 11),\\n",
    "    (\\"乘积\\", (6, 7), 42),\\n",
    "    (\\"乘积\\", (4, 9), 36),\\n",
    "    (\\"取反\\", (9,), -9),\\n",
    "    (\\"平方\\", (4,), 16),\\n",
    "]\\n",
    "\\n",
    "# 工具集：加、减、乘、取反、平方\\n",
    "def tool_add(a, b):\\n",
    "    \\"\\"\\"加法。\\"\\"\\"\\n",
    "    return a + b\\n",
    "\\n",
    "def tool_sub(a, b):\\n",
    "    \\"\\"\\"减法。\\"\\"\\"\\n",
    "    return a - b\\n",
    "\\n",
    "def tool_mul(a, b):\\n",
    "    \\"\\"\\"乘法。\\"\\"\\"\\n",
    "    return a * b\\n",
    "\\n",
    "def tool_neg(a):\\n",
    "    \\"\\"\\"取反。\\"\\"\\"\\n",
    "    return -a\\n",
    "\\n",
    "def tool_sq(a):\\n",
    "    \\"\\"\\"平方。\\"\\"\\"\\n",
    "    return a * a\\n",
    "\\n",
    "TOOLS = {\\"add\\": tool_add, \\"sub\\": tool_sub, \\"mul\\": tool_mul,\\n",
    "         \\"neg\\": tool_neg, \\"sq\\": tool_sq}\\n",
    "\\n",
    "# 第二块：agent。LLM 决定动作，工具执行，评分函数判对错\\n",
    "import re\\n",
    "\\n",
    "def parse_action(text):\\n",
    "    \\"\\"\\"从 LLM 输出解析 Action: name(args)，解析不到返回 None。\\"\\"\\"\\n",
    "    m = re.search(r\\"Action:\\\\s*(\\\\w+)\\\\(([^)]*)\\\\)\\", text)\\n",
    "    if not m:\\n",
    "        return None\\n",
    "    name = m.group(1)\\n",
    "    args = [int(x) for x in re.findall(r\\"-?\\\\d+\\", m.group(2))]\\n",
    "    return name, args\\n",
    "\\n",
    "def make_agent(client):\\n",
    "    \\"\\"\\"构造\\"LLM 决策 + 工具执行\\"的 agent，返回 act(task) 函数。\\n",
    "\\n",
    "    mock 模式返回脚本化动作，其中乘积任务误用加法，制造可观测的失败；\\n",
    "    真实模式由模型输出 Action 行。\\n",
    "    \\"\\"\\"\\n",
    "    mock_plan = {\\"求和\\": \\"add\\", \\"差\\": \\"sub\\", \\"乘积\\": \\"add\\",\\n",
    "                 \\"取反\\": \\"neg\\", \\"平方\\": \\"sq\\"}\\n",
    "\\n",
    "    def act(task):\\n",
    "        name, args, _ = task\\n",
    "        if client.is_mock:\\n",
    "            tool = mock_plan[name]\\n",
    "            return \\"Action: %s(%s)\\" % (tool, \\", \\".join(map(str, args)))\\n",
    "        prompt = (\\"任务：%s，参数 %s。请只输出一行 Action，\\"\\n",
    "                  \\"例如 Action: add(3, 5)。\\" % (name, args))\\n",
    "        return client.chat([{\\"role\\": \\"user\\", \\"content\\": prompt}])\\n",
    "\\n",
    "    return act\\n",
    "\\n",
    "print(\\"任务套件 %d 个，工具 %d 个，agent 已就绪。\\" % (len(TASKS), len(TOOLS)))\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "c8715cb09746",
   "metadata": {},
   "outputs": [],
   "source": [
    "# 第三块：运行。每个任务重复 N_RUNS 次，记录成败、耗时与动作路径\\n",
    "import time\\n",
    "import numpy as np\\n",
    "\\n",
    "np.random.seed(42)\\n",
    "act = make_agent(client)\\n",
    "N_RUNS = 3\\n",
    "rows = []\\n",
    "\\n",
    "def run_once(act, task):\\n",
    "    \\"\\"\\"执行单个任务一次，返回 (是否通过, 输出, 路径, 耗时秒)。\\"\\"\\"\\n",
    "    name, args, expected = task\\n",
    "    t0 = time.time()\\n",
    "    action_text = act(task)\\n",
    "    parsed = parse_action(action_text)\\n",
    "    if parsed is None:\\n",
    "        return False, \\"PARSE_FAIL\\", [(\\"action\\", action_text)], time.time() - t0\\n",
    "    tool_name, tool_args = parsed\\n",
    "    fn = TOOLS.get(tool_name)\\n",
    "    if fn is None:\\n",
    "        return False, \\"TOOL_FAIL\\", [(action_text, None)], time.time() - t0\\n",
    "    result = fn(*tool_args)\\n",
    "    return (result == expected), result, [(action_text, result)], time.time() - t0\\n",
    "\\n",
    "for task in TASKS:\\n",
    "    name, args, expected = task\\n",
    "    for r in range(N_RUNS):\\n",
    "        ok, out, path, dt = run_once(act, task)\\n",
    "        rows.append((name, args, r, ok, out, dt, path))\\n",
    "\\n",
    "print(\\"任务    参数     run   通过   输出      耗时(us)\\")\\n",
    "for name, args, r, ok, out, dt, path in rows:\\n",
    "    print(\\"%-4s %-6s %3d   %s   %5s    %6.0f\\"\\n",
    "          % (name, str(args), r, \\"是\\" if ok else \\"否\\", out, dt * 1e6))\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "b62d6b65d9f0",
   "metadata": {},
   "outputs": [],
   "source": [
    "# 聚合统计：pass@1、平均耗时、成功/失败路径，输出评测报告\\n",
    "import numpy as np\\n",
    "\\n",
    "def aggregate_report(rows):\\n",
    "    \\"\\"\\"把逐条运行记录汇总成按任务的 pass@1 与整体统计。\\"\\"\\"\\n",
    "    by_task = {}\\n",
    "    for name, args, r, ok, out, dt, path in rows:\\n",
    "        by_task.setdefault(name, []).append(ok)\\n",
    "    report = {}\\n",
    "    for name, oks in by_task.items():\\n",
    "        report[name] = {\\"pass_at_1\\": float(np.mean(oks)), \\"runs\\": len(oks)}\\n",
    "    overall = float(np.mean([v[\\"pass_at_1\\"] for v in report.values()]))\\n",
    "    mean_time = float(np.mean([row[5] for row in rows]))\\n",
    "    return report, overall, mean_time\\n",
    "\\n",
    "report, overall, mean_time = aggregate_report(rows)\\n",
    "\\n",
    "print(\\"== 评测报告 ==\\")\\n",
    "for name, v in report.items():\\n",
    "    print(\\"%-4s pass@1 = %.2f  （%d 次运行）\\" % (name, v[\\"pass_at_1\\"], v[\\"runs\\"]))\\n",
    "print(\\"总体 pass@1 = %.2f，平均耗时 %.0f us（toy 任务接近瞬时）\\"\\n",
    "      % (overall, mean_time * 1e6))\\n",
    "\\n",
    "# 取一个成功与一个失败的记录，打印各自的动作路径\\n",
    "expected_map = {}\\n",
    "for name, args, exp in TASKS:\\n",
    "    expected_map.setdefault(name, exp)\\n",
    "\\n",
    "ok_row = next(row for row in rows if row[3])\\n",
    "bad_row = next(row for row in rows if not row[3])\\n",
    "print(\\"成功路径：%s -> %s\\" % (ok_row[6][0][0], ok_row[4]))\\n",
    "print(\\"失败路径：%s -> %s（期望 %s）\\"\\n",
    "      % (bad_row[6][0][0], bad_row[4], expected_map[bad_row[0]]))\\n",
    "print(\\"关键观察：报告把'乘积'任务的系统性失败暴露出来，这正是评测的目的。\\")\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "8005171c73b2",
   "metadata": {},
   "source": [
    "## 小结\\n",
    "\\n",
    "这一节所学的要点：\\n",
    "\\n",
    "- [ ] 评测 Agent 由任务环境、评分函数、聚合统计三块组成，各自的模糊之处构成评测的难点\\n",
    "- [ ] 选择题分数会饱和、跨 benchmark 不可比、离真实任务远，无法翻译成\\"能做完几小时的工作\\"\\n",
    "- [ ] METR 用人类完成时长标度任务难度，logistic 拟合出的 50% 时间视野是能力的统一标尺\\n",
    "- [ ] 时间视野的 log 值随发布日期近似线性增长，斜率可换算成\\"翻倍天数\\"\\n",
    "- [ ] GDPval 用真实经济任务盲评 win rate，平手计 0.5；自动评分器要和人类间一致率对齐\\n",
    "- [ ] try-n-then-fix 的期望成本把 win rate 换算成期望耗时，win rate 不够高时模型协助反而更慢\\n",
    "- [ ] LLM judge 有位置偏差，成对交换顺序取平均可以抵消\\n",
    "- [ ] 检索质量用 RR、RC、可验证性刻画，几何平均惩罚单项短板\\n",
    "- [ ] 一个最小 harness = 任务套件 + agent 运行器 + 聚合报告，先跑通再谈指标\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "136c09fe5914",
   "metadata": {},
   "source": [
    "## 作业\\n",
    "\\n",
    "> 可以让 AI 帮忙解释思路，但不建议直接让 AI \\"做完这道题\\"。\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "232d2440d6de",
   "metadata": {},
   "source": [
    "**作业 1：拟合 50% 时间视野**\\n",
    "\\n",
    "下表是一批任务的人类时长与某模型的成功率，真实 50% 时间视野为 1.0 小时。用 scipy 拟合 logistic，求出 h 与 beta，并断言拟合的 h 接近 1.0、且在该时长处预测成功率接近 0.5。\\n",
    "\\n",
    "小提示：先对 task_times 取 log 再拟合；目标函数是负对数似然。\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "63d1a7feb16d",
   "metadata": {},
   "outputs": [],
   "source": [
    "# 作业 1：拟合 time horizon\\n",
    "import numpy as np\\n",
    "from scipy.optimize import minimize\\n",
    "\\n",
    "task_times = np.array([0.1, 0.3, 0.6, 1.0, 1.5, 2.5, 4.0, 7.0])   # 人类时长（小时）\\n",
    "success = np.array([0.96, 0.84, 0.72, 0.52, 0.40, 0.28, 0.16, 0.10])\\n",
    "\\n",
    "def neg_log_likelihood(params, log_t, y):\\n",
    "    log_h, beta = params\\n",
    "    p = 1.0 / (1.0 + np.exp(-(log_h - log_t) * beta))     # 填空 1：logistic 模型\\n",
    "    eps = 1e-12\\n",
    "    return -np.sum(y * np.log(p + eps) + (1 - y) * np.log(1 - p + eps))\\n",
    "\\n",
    "log_t = np.log(task_times)\\n",
    "res = minimize(neg_log_likelihood, [0.0, 2.0], args=(log_t, success),\\n",
    "               method=\\"L-BFGS-B\\", bounds=[(-5.0, 5.0), (0.1, 10.0)])\\n",
    "log_h_est, beta_est = res.x\\n",
    "h_est = np.exp(log_h_est)                                  # 填空 2：还原时间视野\\n",
    "\\n",
    "assert abs(h_est - 1.0) < 0.3, \\"h 应接近 1.0 小时\\"\\n",
    "p_at_true = 1.0 / (1.0 + np.exp(-(log_h_est - np.log(1.0)) * beta_est))\\n",
    "assert abs(p_at_true - 0.5) < 0.1, \\"在真实视野处成功率应接近 0.5\\"\\n",
    "print(\\"拟合 h = %.2f 小时（真实 1.0），beta = %.2f\\" % (h_est, beta_est))\\n",
    "print(\\"在 t=1.0h 处预测成功率 = %.3f\\" % p_at_true)\\n",
    "print(\\"通过：把成功率与人类时长一起拟合，就还原出模型的能力标尺。\\")\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "5a980c9df5b7",
   "metadata": {},
   "source": [
    "**作业 2：win rate 与一致率**\\n",
    "\\n",
    "下表是模型、两位人类专家在 8 个任务上的三值打分。计算模型的 win rate、自动评分器与人类的一致率 A_HA、人类间一致率 A_HH，并断言 A_HA 低于 A_HH。\\n",
    "\\n",
    "小提示：win rate 是打分的均值（平手计 0.5）；一致率是 1 - |x - y| 的均值，A_HA 里人类打分取两位专家的平均。\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "be7ec6d27154",
   "metadata": {},
   "outputs": [],
   "source": [
    "# 作业 2：win rate 与一致率\\n",
    "import numpy as np\\n",
    "\\n",
    "model = np.array([1.0, 0.5, 0.0, 1.0, 0.5, 0.0, 1.0, 0.5])\\n",
    "human1 = np.array([1.0, 0.5, 0.5, 0.5, 0.5, 0.0, 1.0, 1.0])\\n",
    "human2 = np.array([1.0, 1.0, 0.5, 0.5, 0.5, 0.0, 1.0, 0.5])\\n",
    "\\n",
    "win_rate = np.mean(model)                              # 填空 1：win rate\\n",
    "\\n",
    "def agreement(x, y):\\n",
    "    \\"\\"\\"两条评分的一致率 1 - |x - y|。\\"\\"\\"\\n",
    "    return np.mean(1.0 - np.abs(x - y))                # 填空 2\\n",
    "\\n",
    "A_HA = agreement(model, (human1 + human2) / 2.0)\\n",
    "A_HH = agreement(human1, human2)\\n",
    "\\n",
    "assert abs(win_rate - 0.5625) < 1e-9, \\"win rate 应为 0.5625\\"\\n",
    "assert A_HA < A_HH, \\"自动评分器应比人类彼此间更不一致\\"\\n",
    "print(\\"win rate = %.3f\\" % win_rate)\\n",
    "print(\\"A_HA = %.3f，A_HH = %.3f\\" % (A_HA, A_HH))\\n",
    "print(\\"通过：评测自动评分器要拿'人类彼此间的一致率'当基线。\\")\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "fe92d538d22a",
   "metadata": {},
   "source": [
    "**作业 3：nugget 覆盖**\\n",
    "\\n",
    "人类 exemplar 由 5 个原子事实（nugget）构成，两份生成报告由句子列表给出。实现 coverage 函数，判断每个 nugget 是否作为子串出现在报告里，并断言报告 B 的覆盖更高。\\n",
    "\\n",
    "小提示：nugget 是原子事实级别的要点；用 \`n in text\` 判断子串即可。\\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "195261649d27",
   "metadata": {},
   "outputs": [],
   "source": [
    "# 作业 3：nugget 覆盖\\n",
    "import numpy as np\\n",
    "\\n",
    "nuggets = [\\"引力\\", \\"等效原理\\", \\"光线弯曲\\", \\"时间膨胀\\", \\"引力波\\"]\\n",
    "report_a = [\\"引力使苹果落地\\", \\"等效原理描述局部等价\\"]\\n",
    "report_b = [\\"引力使苹果落地\\", \\"等效原理描述局部等价\\", \\"光线弯曲被观测验证\\",\\n",
    "            \\"强场下时间膨胀显著\\", \\"引力波由双星并合产生\\"]\\n",
    "\\n",
    "def coverage(nuggets, report):\\n",
    "    \\"\\"\\"计算报告对 nugget 集合的覆盖比例。\\"\\"\\"\\n",
    "    text = \\"\\".join(report)\\n",
    "    return np.mean([1.0 if n in text else 0.0 for n in nuggets])   # 填空\\n",
    "\\n",
    "cov_a = coverage(nuggets, report_a)\\n",
    "cov_b = coverage(nuggets, report_b)\\n",
    "\\n",
    "assert cov_b > cov_a, \\"报告 B 覆盖更全，分应更高\\"\\n",
    "print(\\"报告 A nugget 覆盖 = %.2f\\" % cov_a)\\n",
    "print(\\"报告 B nugget 覆盖 = %.2f\\" % cov_b)\\n",
    "print(\\"通过：nugget 覆盖把开放式综述压缩成可自动判定的召回率。\\")\\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "06a3e7c78fa6",
   "metadata": {},
   "source": [
    "## 参考资料\\n",
    "\\n",
    "- METR, [Measuring AI Ability to Complete Long Software Tasks](https://arxiv.org/abs/2503.14499), 2025 — 提出 50% 时间视野指标，用人类时长标度 Agent 能力，发现前沿模型约每 7 个月翻倍\\n",
    "- OpenAI, [GDPval: Evaluating AI Model Performance on Real-World Economically Valuable Tasks](https://arxiv.org/abs/2510.04374), 2025 — 9 大行业 44 个职业的真实经济任务，盲评 win rate 显示前沿模型已逼近人类专家\\n",
    "- Patel et al., [DeepScholar-Bench: A Live Benchmark and Automated Evaluation for Generative Research Synthesis](https://arxiv.org/abs/2508.20033), 2025 — 三维 7 指标自动评测深度研究，所有系统几何平均都低于 31%\\n",
    "- Wijk et al., [RE-Bench: Evaluating Frontier AI R&D Capabilities of Language Model Agents Against Human Experts](https://arxiv.org/abs/2411.15114), 2024 — 8 小时机器学习研究任务与人类专家基线，METR 任务套件的来源之一\\n",
    "- Panickssery, Bowman & Feng, [LLM Evaluators Recognize and Favor Their Own Generations](https://arxiv.org/abs/2404.13076), 2024 — LLM judge 识别并偏爱自己生成的输出，自动评分器偏见的证据\\n",
    "- HCAST: Human-Calibrated Autonomy Software Tasks（2025，论文即将发布）— METR 任务套件的主力，覆盖 1 分钟到 30 小时的常规软件任务\\n",
    "- Ngo, [Clarifying and Predicting AGI](https://www.alignmentforum.org/posts/5pRbXo6EEnRBSHokF/clarifying-and-predicting-agi), 2023 — \\"1 个月 AGI（167 小时）\\"定义与能力外推思路的来源\\n",
    "- OpenAI, [evals.openai.com](https://evals.openai.com) — GDPval 的 220 个 gold 任务与实验性自动评分服务入口\\n",
    "- [DeepScholar-Bench 官方仓库](https://github.com/guestrin-lab/deepscholar-bench) — live 数据管道与评测代码\\n",
    "- Stanford, [CS329A 课程大纲](https://cs329a.stanford.edu/) — 本讲在课程地图中的定位\\n"
   ]
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language": "python",
   "name": "python3"
  },
  "language_info": {
   "name": "python",
   "version": "3.13"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 5
}`;export{n as default};
