# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# 论文 PDF 体积过大（共约 150MB），不纳入版本控制。
# 用 scripts/download_papers.py 可一键重新下载；研读笔记 NOTES.md 已入库。
papers/lecture-*/**/*.pdf
papers/**/*.pdf

# 论文临时文本提取
papers/lecture-*/**/*.txt

# 本地执行产物
*.log

# 前端
web/node_modules/
web/dist/

# GitHub Pages 构建产物（部署源为 main 分支的 /docs 目录，需提交）
# 若改用 GitHub Actions 工作流自动构建，可取消提交并忽略 docs/
